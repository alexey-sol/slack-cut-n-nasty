type User {
    id: Int!
    name: String
    workspaces: [Workspace]
}

input CreateUserInput {
    name: String!
}

type Query {
    getUser(id: Int!): User
}

type Mutation {
    createUser(input: CreateUserInput): User
}
